# คู่มือการนำระบบ NIT Inventory ขึ้นเซิร์ฟเวอร์จริง (Production Deployment Guide)

คู่มือนี้จะแนะนำขั้นตอนการนำระบบระบบตัดยอดคลังและแจ้งเตือนเวชภัณฑ์ (NIT Inventory) ขึ้นระบบจริง ซึ่งแบ่งออกเป็น 2 ส่วนหลัก:
1. **Frontend (หน้าเว็บ)**: นำขึ้น **Firebase Hosting** (เนื่องจากโครงการใช้บริการ Firebase Firestore อยู่แล้ว)
2. **Backend (เซิร์ฟเวอร์ส่งเมลและแปลงไฟล์ Excel)**: นำขึ้น **Render** (บริการฟรีและเหมาะสำหรับ Node.js Express)

---

## ส่วนที่ 1: การเตรียมโค้ดฝั่ง Frontend (ย้ายจาก Local Host)

ก่อนนำเว็บขึ้นระบบจริง ต้องเปลี่ยนให้ตัวเว็บเรียกใช้ API จากเซิร์ฟเวอร์จริง ไม่ใช่เครื่องตัวเอง (localhost)

1. เปิดไฟล์ `assets/js/import.js` และ `assets/js/notifications.js`
2. เปลี่ยนตัวแปร `MAIL_SERVER_URL` จาก `http://localhost:3001` ให้เป็น URL เซิร์ฟเวอร์จริงของ **Render** (ที่ได้จากขั้นตอนส่วนที่ 3 เช่น `https://nit-inventory-api.onrender.com`)
   ```javascript
   // เปลี่ยนจาก:
   // const MAIL_SERVER_URL = 'http://localhost:3001';
   // เป็น URL จริง:
   const MAIL_SERVER_URL = 'https://YOUR-BACKEND-NAME.onrender.com';
   ```

---

## ส่วนที่ 2: การนำ Frontend ขึ้น Firebase Hosting (ฟรี)

เนื่องจากระบบมีการใช้ Firebase config อยู่แล้วใน `assets/js/firebase-config.js` การใช้ Firebase Hosting จึงเป็นวิธีที่ดีที่สุด

### ขั้นตอนการตั้งค่า:
1. ติดตั้ง **Firebase CLI** บนเครื่องของคุณ (รันใน Terminal):
   ```bash
   npm install -g firebase-tools
   ```
2. ล็อกอินเข้าใช้งานบัญชี Firebase:
   ```bash
   firebase login
   ```
3. เริ่มต้นโปรเจกต์ Hosting ในโฟลเดอร์หลักของโปรเจกต์ (โฟลเดอร์ `MMI340 project`):
   ```bash
   firebase init hosting
   ```
   **เมื่อระบบถามคำถาม ให้ตอบดังนี้:**
   * **Select a project option**: เลือก `Use an existing project`
   * **Select your project**: เลือก `opd-neuro-inventory` (โปรเจกต์ของระบบนี้)
   * **What do you want to use as your public directory?**: กด Enter เพื่อใช้ค่าเริ่มต้น หรือพิมพ์ `.` (จุด) เนื่องจากไฟล์ `index.html` อยู่ที่โฟลเดอร์หลัก
   * **Configure as a single-page app?**: ตอบ `No` (เนื่องจากเรามีหลายหน้าเช่น `index.html` และ `poster.html`)
   * **Set up automatic builds and deploys with GitHub?**: ตอบ `No` (เว้นแต่ต้องการผูก Git)
   * **File index.html already exists. Overwrite?**: ตอบ `No` (ห้ามเขียนทับไฟล์เดิมเด็ดขาด)

4. เมื่อตั้งค่าสำเร็จ ให้รันคำสั่งเพื่ออัปโหลดหน้าเว็บขึ้นระบบจริง:
   ```bash
   firebase deploy --only hosting
   ```
   *หลังทำเสร็จ คุณจะได้ URL หน้าเว็บจริง เช่น `https://opd-neuro-inventory.web.app`*

---

## ส่วนที่ 3: การนำ Backend (Node.js) ขึ้น Render (ฟรี)

**Render.com** เป็นคลาวด์สำหรับรัน Node.js API Server ฟรีและใช้งานง่ายมาก

### ขั้นตอนการนำขึ้นระบบ:
1. นำโค้ดโปรเจกต์นี้ขึ้น **GitHub Repository** ของคุณ (ซึ่งปัจจุบันอยู่ใน GitHub อยู่แล้ว)
2. สมัครใช้งานและล็อกอินที่ [Render.com](https://render.com)
3. กดปุ่ม **New +** > เลือก **Web Service**
4. เชื่อมโยงบัญชี GitHub ของคุณ และเลือก Repository **NIT-inventory-web**
5. **ตั้งค่าข้อมูลบริการ (Web Service Settings):**
   * **Name**: `nit-inventory-api` (หรือชื่ออื่นๆ ที่ต้องการ)
   * **Region**: เลือก `Singapore` (ใกล้ไทยที่สุด เพื่อความเร็ว)
   * **Branch**: `main`
   * **Root Directory**: พิมพ์ `server` (สำคัญมาก! เพราะโค้ด Express อยู่ในโฟลเดอร์นี้)
   * **Runtime**: `Node`
   * **Build Command**: `npm install`
   * **Start Command**: `npm start`
   * **Instance Type**: เลือกแบบ `Free`

6. **เพิ่มตัวแปรสภาพแวดล้อม (Environment Variables):**
   กดที่ปุ่ม **Advanced** หรือไปที่แท็บ **Environment** แล้วกด **Add Environment Variable** นำค่าจากไฟล์ `server/.env` ไปใส่:
   * `PORT`: `3001` (หรือปล่อยว่างไว้เพื่อให้ระบบสุ่มพอร์ตจริง)
   * `GMAIL_USER`: `tinnapop342@gmail.com` (Gmail ที่ใช้ส่งเมลแจ้งเตือน)
   * `GMAIL_PASS`: `iltu rwpj wiyt wpjx` (App Password ของ Gmail)
   * `ALLOWED_ORIGINS`: `*` (หรือพิมพ์ URL หน้าเว็บของคุณจาก Firebase Hosting เช่น `https://opd-neuro-inventory.web.app` เพื่อความปลอดภัย)

7. กดปุ่ม **Create Web Service** เพื่อเริ่มสร้างเซิร์ฟเวอร์
   *รอประมาณ 2-3 นาที ระบบจะ Build และ Deploy สำเร็จ คุณจะได้ URL ของหลังบ้าน เช่น `https://nit-inventory-api.onrender.com`*
   *ให้นำ URL นี้ย้อนกลับไปใส่ในตัวแปร `MAIL_SERVER_URL` ในโฟลเดอร์ Frontend แล้วทำ `firebase deploy` อีกครั้งหนึ่ง*

---

## ⚠️ ข้อควรระวังเพิ่มเติมในการใช้งานจริง

1. **การปิดกั้นอีเมล SMTP (Gmail App Password)**:
   - บัญชี Gmail ที่ระบุใน `GMAIL_USER` ต้องเปิดการยืนยันตัวตน 2 ขั้นตอน (2-Step Verification) และใช้ **App Password** ในช่อง `GMAIL_PASS` เท่านั้น (ห้ามใส่รหัสผ่าน Gmail ปกติ)
2. **การเรียกใช้ Firebase Admin SDK (หลังบ้าน)**:
   - ไฟล์ `server/serviceAccountKey.json` ต้องมีอยู่ในการอัปโหลดขึ้น Git เพื่อให้ API Server สามารถเชื่อมต่อข้อมูล Firebase หลังบ้านได้ (ตรวจสอบให้แน่ใจว่าไฟล์นี้ถูก Commit ขึ้น Git แล้ว)
3. **การหลับของเซิร์ฟเวอร์ฟรี (Render Cold Start)**:
   - เนื่องจากเป็นแพลน Free ของ Render หลังบ้านจะ "หลับ" ไปหากไม่มีการเข้าใช้งานนานกว่า 15 นาที เมื่อมีคนกดใช้ปุ่มนำเข้าข้อมูลครั้งแรกหลังจากเครื่องหลับ ระบบอาจใช้เวลา 30-50 วินาทีในการตื่นขึ้นมาตอบสนอง (Cold Start) ซึ่งเป็นเรื่องปกติของคลาวด์ฟรี
