# คู่มือการติดตั้งและใช้งานระบบคลังพัสดุ (NIT Inventory)

ระบบปฏิบัติการบริหารจัดการคลังพัสดุและครุภัณฑ์ขนาดเล็ก ที่เขียนด้วยหน้าเว็บไซต์ที่ใช้งานได้ทันที (Static HTML) และมีการเชื่อมต่อกับระบบฐานข้อมูลคลาวด์ตลอดเวลา!

---

##  สิ่งที่ต้องเตรียมก่อนเริ่มใช้งาน
ระบบนี้มีการตรวจสอบความปลอดภัย (Authentication) และเก็บข้อมูลผู้ใช้งาน คุณจำเป็นต้องอนุญาตให้ระบบมีฟังก์ชันล็อกอินผ่าน Firebase Console ด้วยขั้นตอนดังนี้:

### ขันตอนที่ 1: การเปิดฐานข้อมูล Firebase (คุณน่าจะทำแล้ว)
นำ `firebaseConfig` จากเว็บ Firebase ของคุณมาใส่ในไฟล์ `assets/js/firebase-config.js` ตามปกติ 

### ขั้นตอนที่ 2: เปิดสวิตช์ระบบสมัครสมาชิก (Authentication) **[สำคัญมาก]**
1. เข้าไปที่ [Firebase Console](https://console.firebase.google.com/) เลือกโปรเจกต์คุณ (opd-neuro-inventory)
2. แถบเมนูด้านซ้าย มองหา **Build** แล้วเลือก **Authentication**
3. กดปุ่ม **"Get Started"**
4. หน้าต่าง Sign-in method ให้คุณคลิกเข้าไปที่บรรทัด **"Email/Password"**
5. กดโยกสวิตซ์อันแรก ("Enable" Email/Password) แล้วกด **Save**
6. เสร็จสิ้น! ตอนนี้คุณก็สามารถเปิดไฟล์เว็บตัวโปรเจกต์ `index.html` แล้วทดลองพิมพ์สมัครสมาชิกได้เลยครับ

### ขั้นตอนที่ 3: วิธีเสกตัวเองเป็น Super Admin 👑
ระบบถูกออกแบบมาให้คนทั่วไปที่สมัครใหม่เป็นได้แค่ **"user" (พนักงานทั่วไป)** เท่านั้น เพื่อป้องกันคนแปลกหน้ามาล้างบางระบบ หากคุณต้องการสิทธิเป็นคนคุม (Super Admin) ให้ทำแบบนี้ครับ:
1. ให้คุณเอาอีเมลตัวเองเปิดเว็บ `index.html` แล้วคลิกปุ่ม **"สมัครสมาชิก"** ไปตามปกติ
2. พอสมัครเสร็จ **(อย่าเพิ่งทำอะไรในเว็บ)** ให้คุณกลับไปที่หน้าจอ *Firebase Console* บนเน็ต
3. ไปที่เมนูด้านซ้าย เลือก **Build > Firestore Database**
4. ลองมองหา Collection ย่อยตรงกลางจอที่ชื่อว่า **`users`** นอกเหนือจาก products และ logs
5. คุณจะพบข้อมูลชื่อคุณที่คุณเพิ่งสมัครไป ให้มองหาบรรทัดที่ขียวว่า `role: "user"`
6. ให้คุณคลิกตรงคำว่า `"user"` แล้วพิมพ์เปลี่ยนเป็นคำว่า `"superadmin"` (พิมพ์เล็กทั้งหมด ไม่มีวรรค) แล้วเคาะ Enter หรือกดปุ่ม Update
7. หลังจากแก้เสร็จสับ ให้คุณกลับมาที่หน้าเว็บโปรเจกต์ของเรา กด **"ออกจากระบบ (Logout)"** และ **"เข้าสู่ระบบ" ใหม่อีกรอบ!**
8. บู้ม! 🚀 เมนูจัดการต่างๆ จะโผล่ขึ้นมาให้คุณใช้งานครบทุกฟังก์ชันครับ

---

## 📂 โครงสร้างโฟลเดอร์ของโปรเจกต์
- `index.html` : เป็นไฟล์หน้าต่างหลัก องค์ประกอบปุ่มทั้งหมดอยู่ที่นี่
- `assets/`
  - `css/` -> ตกแต่งหน้าตา (Tailwind CSS และปรับแต่งเอง)
  - `js/` (แยกเป็นหลายๆ โมดูลเพื่อความสะดวกในการจัดการ)
    - `firebase-config.js` : ศูนย์รวมกุญแจ API ของระบบ 🔑
    - `auth.js` : สมองส่วนล็อกอิน ควบคุมเวลาเข้า-ออกระบบและสิทธิ์ของผู้ใช้งาน
    - `state.js` : จัดการข้อมูล สถานะ คลังสินค้า ผ่านระบบ Real-time
    - `nav.js` : ควบคุมหน้าตา UI ตอนสลับเมนูหรือสลับ Role
    - `render.js` : จัดการตรรกะการวาดตารางและกราฟย้อนหลัง
    - `actions.js` : ควบคุมปุ่มกด การยืม คืน รับของทั้งฝั่งผู้ใช้และฝั่งแอดมิน

---

## 🔑 ระบบสิทธิ์ (Roles) และการล็อกอิน
ระบบได้ยกระดับมาใช้การล็อกอินจริงๆ แล้ว ผู้ที่เข้ามาใหม่ทั้งหมดจะมีสิทธิ์ระดับล่างสุดจนกว่าหัวหน้าจะปรับระดับให้ในฐานข้อมูล
- **Super Admin (`superadmin`)**: เห็นหมด ทำได้ทุกอย่าง
- **Admin (`admin`)**: เบิก ยืม คืน รับพัสดุ เพิ่ม/ลบ ทรัพยากรได้ *แต่ไม่อนุญาตให้ทำลายระบบ*
- **User (`user`)**: เห็นข้อมูลเฉพาะส่วนการยืม-เบิก มองดูประวัติของตัวเองเท่านั้น
cobj emdb kdgw joxz